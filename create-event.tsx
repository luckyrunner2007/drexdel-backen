import { View, Text } from 'react-native';

export default function CreateEventPage() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Create Event (Coming Soon)</Text>
    </View>
  );
}