import { MapRadarScreen } from '../src/screens/Discovery/MapRadarScreen';

/**
 * PROJECT DREXDEL - PRESENCE RADAR ROUTE
 * Opens the live GPS friend radar + presence privacy controls as a modal.
 */
export default function PresenceModal() {
  return <MapRadarScreen />;
}