import { View, Text } from 'react-native';

export const GateScannerView = () => (
  <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
    <Text>Scanner not available on web</Text>
  </View>
);

export default GateScannerView;